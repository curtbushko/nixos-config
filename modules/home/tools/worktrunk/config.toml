# Worktrunk user configuration

# Worktree path - creates worktrees as siblings of .bare
worktree-path = "{{ repo_path }}/../{{ branch }}"

# Custom aliases - available globally
[aliases]
checkout = "wt switch --remotes {{ args }}"
